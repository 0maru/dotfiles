{
	"private": true,
	"scripts": {
		"install:chromium": "playwright install chromium",
		"install:browsers": "playwright install chromium firefox webkit",
		"update": "pnpm up playwright@latest && pnpm exec playwright install chromium"
	},
	"devDependencies": {
		"playwright": "^1.60.0"
	}
}
